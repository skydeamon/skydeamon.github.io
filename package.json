{
  "name": "skydeamon-github-io",
  "version": "2.0.0",
  "private": true,
  "description": "Jade Makwela portfolio site — static HTML/CSS/JS",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "test:deployed": "node --test \"tests/deployed/*.test.js\""
  }
}