{
  "name": "skydeamon-testenv-integration",
  "version": "1.0.0",
  "private": true,
  "description": "Integration test venv (Playwright against local server).",
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}